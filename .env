# NEXT_PUBLIC_API_URL="http://localhost:4000/api/waitlist"
# NEXT_PUBLIC_API_URL="http://localhost:4000/api"
NEXT_PUBLIC_API_URL="https://frostfix-backend-v2.onrender.com/api"

# Points at a local backend checkout with the booking-system branch — the
# deployed instance above doesn't have the new routes until that PR merges.
NEXT_PUBLIC_API_BASE_URL="http://localhost:4000"