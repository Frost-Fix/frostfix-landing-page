NEXT_PUBLIC_API_URL="http://localhost:4000/api/waitlist"

# Base URL of the frostfix-backend-v2 API — the booking/auth/admin/contractor
# pages call this directly from the browser (CORS is open on the backend).
NEXT_PUBLIC_API_BASE_URL="http://localhost:4000"